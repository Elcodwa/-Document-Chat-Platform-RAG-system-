# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3      # PostgreSQL driver (app DB + Postgres connections)
pymysql==1.1.1              # MySQL driver (MySQL connections)
pgvector==0.3.6             # pgvector <-> SQLAlchemy integration

# --- Security ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
cryptography==44.0.0

# --- SQL parsing / validation ---
sqlglot==26.0.1

# --- LLM + agent orchestration ---
groq==0.13.1
langchain-groq==0.2.4
langgraph==0.2.60

# --- Embeddings (local, CPU, no GPU/torch needed) ---
fastembed==0.5.1

# --- Document parsing ---
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5
pandas==2.2.3

# --- Utilities ---
httpx==0.28.1
tenacity==9.0.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
faker==33.1.0
